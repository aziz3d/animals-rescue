@props(['comment', 'depth'])<div class="bg-white border border-gray-200 rounded-lg p-6 {{ $depth > 0 ? 'ml-' . min($depth * 4, 16) . ' border-t-2 border-l-2' : '' }}">    <div class="flex items-center justify-between mb-4">        <div class="flex items-center">            <div class="bg-amber-100 rounded-full w-10 h-10 flex items-center justify-center">                <span class="text-amber-800 font-bold">{{ strtoupper(substr($comment->commenter_name, 0, 1)) }}</span>            </div>            <div class="ml-4">                <h4 class="font-semibold text-gray-900">{{ $comment->commenter_name }}</h4>                <p class="text-sm text-gray-500">{{ $comment->created_at->format('F j, Y \\\\ t g:i A') }}</p>            </div>        </div>    </div>   <div class="text-gray-700 mb-4 whitespace-pre-line">        {{ $comment->content }}    </div>    @auth        <div class="flex items-center">            <button                wire:click="reply({{ $comment->id }})"                class="text-amber-600 hover:text-amber-800 text-sm font-medium">               Reply            </button>       </div>        <!-- Reply Form -->      @if($replyingTo == $comment->id)           <div class="mt-4 p-4 bg-amber-50 rounded-lg">                <form wire:submit="saveReply" class="space-y-4">                    <div class="flex items-center mb-4">                        <div class="bg-amber-100 rounded-full w-8 h-8 flex items-center justify-center"> <span class="text-amber-800 font-bold text-sm">{{ strtoupper(substr(Auth::user()->name, 0, 1)) }}</span>                        </div>               <div class="ml-3">                            <h4 class="font-semibold text-gray-900 text-sm">{{ Auth::user()->name }}</h4>                           <p class="text-xs text-gray-500">{{ Auth::user()->email }}</p>                        </div>                   </div>                    <div>                     <label for="replyContent" class="block text-sm font-medium text-gray-700 mb-1">Reply *</label>                        <textarea wire:model="replyContent" id="replyContent"                             rows="3"                            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-amber-500"                            placeholder="Write your reply..."                 ></textarea>                       @error('replyContent') <span class="text-red-500 text-sm">{{ $message }}</span> @enderror                  </div>                    <div class="flex space-x-2">                       <button                           type="submit"                          class="bg-amber-600 text-white px-4 py-2 rounded-lg hover:bg-amber-700 transition-colors duration-200 text-sm"                      >                           Post Reply                        </button>                     <button                         type="button\"                           wire:click="cancelReply"                           class="bg-gray-200 text-gray-800 px-4 py-2 rounded-lg hover:bg-gray-300 transition-colors duration-200 text-sm"                       >                            Cancel                      </button>                   </div>              </form>            </div>       @endif   @else       <div class="flex items-center">            <a href="{{ route('login') }}" class="text-amber-600 hover:text-amber-800 text-sm font-medium">                Login to reply            </a>        </div>   @endauth    <!-- Nested Comments -->    @if($comment->children && $comment->children->count() > 0)       <div class="mt-6 space-y-6">            @foreach($comment->children as $childComment)               @include('livewire.stories.comment', ['comment' => $childComment, 'depth' => $depth + 1])            @endforeach       </div>    @endif</div>